name: Repository Hygiene

on: [push, pull_request]

permissions:
  contents: read

jobs:
  tracked-files:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Reject local-only and generated files
        run: node scripts/check-repository-files.mjs
